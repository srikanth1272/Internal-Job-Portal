@model IEnumerable<IJPMvcApp.Models.JobSkill>

@{
    ViewData["Title"] = "DetailsByJobId";
}
<div class="container">
<h1>DetailsByJobId</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JobId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SkillId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Experience)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JobId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SkillId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Experience)
            </td>
        </tr>
            <div>
                    <a asp-action="Index" class="btn btn-primary" style="margin-top: 15px;margin-bottom:15px"s>Back to List</a>
            </div>
}
    </tbody>
</table>
</div>